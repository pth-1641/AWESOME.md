export enum EToolkitType {
  TEXT = 'text',
  TECH = 'tech',
  STATS = 'stats',
  SOCIAL = 'social',
  VIEWS = 'views',
}

export enum EAlign {
  LEFT = 'left',
  CENTER = 'center',
  RIGHT = 'right',
}
