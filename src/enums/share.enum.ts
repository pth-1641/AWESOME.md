export enum EToolkitType {
  TEXT = 'text',
  TECH = 'tech',
  STATS = 'stats',
  SOCIAL = 'social',
  VIEWS = 'views',
  MEME = 'meme',
  QUOTE = 'quote',
  IMAGE = 'image',
}

export enum EAlign {
  LEFT = 'left',
  CENTER = 'center',
  RIGHT = 'right',
}
